import { Container, Row } from "react-bootstrap";

const Footer = () => {
  return (
    <Container className="footer bg-info" fluid>
      <Row>
        <span>Footer</span>
      </Row>
    </Container>
  );
};

export default Footer;
