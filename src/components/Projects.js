import { Container, Row } from "react-bootstrap";

const Projects = () => {
  return (
    <Container className="contain" fluid>
      <Row>
        <span>Projects</span>
      </Row>
    </Container>
  );
};

export default Projects;
